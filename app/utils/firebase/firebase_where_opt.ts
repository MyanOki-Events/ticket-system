export enum WhereOpt {
    WHERE_EQUAL = "==",
    WHERE_NOT_EQUAL = "!=",
    WHERE_GREATER_THAN = ">",
    WHERE_GREATER_THAN_EQUAL = ">=",
    WHERE_LESS_THAN = "<",
    WHERE_LESS_THAN_EQUAL = "<=",
    WHERE_IN = "in",
    WHERE_NOT_IN = "not-in",
    WHERE_ARRAY_CONTAINS = "array-contains",
    WHERE_ARRAY_CONTAINS_ANY = "array-contains-any"
}