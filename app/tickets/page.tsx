"use client";

//import { useSession } from "next-auth/react";

const TicketsPage = () => {
//   const { data: session } = useSession();

//   if (!session) {
//     return <p>You need to be logged in to view this page.</p>;
//   }

  return (
    <div>
      <h1>Your Tickets</h1>
      {/* Fetch and display tickets here */}
    </div>
  );
};

export default TicketsPage;
